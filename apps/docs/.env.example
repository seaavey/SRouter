# Copy to `.env` for a stable named tunnel.
# Get the token from the Cloudflare Zero Trust dashboard.
CLOUDFLARE_TUNNEL_TOKEN=

# Canonical URL used by the SEO metadata during the Docker build.
PUBLIC_SITE_URL=https://srouter.web.id
